#include <bits/stdc++.h>

using namespace std;

#define MAXN 102

int a[MAXN], b[MAXN], c[MAXN];

int main() {
    int n, m, x;
    for (int i = 0; i < n; i++)
    {
        cin >> a[i] >> b[i] >> c[i];
    }
    
    return 0;
}