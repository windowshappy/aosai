#include <bits/stdc++.h>
#define AKIOI int
using namespace std ;
AKIOI main()
{
	freopen("colour.in", "r", stdin) ;
	freopen("colour.out", "w", stdout) ;
	srand(time(0)) ;
	cout << (rand() + 1) % 1000 ;
	fclose(stdin) ; 
	fclose(stdout) ;
	return 0 ;
}

